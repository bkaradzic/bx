shallow_clone: true

os:
  - Visual Studio 2015

environment:
  matrix:
  - TOOLSET: vs2015

configuration:
  - Debug
  - Release

install:
  tools\bin\windows\genie %TOOLSET%

build:
  project: .build/projects/$(TOOLSET)/bx.sln
